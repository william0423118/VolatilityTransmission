function datanew=chooseeff(Data)
data=Data([23:23:end],:,:);
Num=size(data,3);
Do=[];
for i=1:Num
Test=CancelZro(data(:,:,i));
Test=diff(log(Test));
try
Resduals(:,:)=GetRes(Test);
catch
end
if 

end

for i=1:Num
    if mean(mean(Resduals(:,:,i)))~=0
        Do=[Do,i];
    end
end
datanew=data(:,:,Do);

A=[];
if A==[]
    A=1;
end